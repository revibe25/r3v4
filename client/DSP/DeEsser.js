export class DeEsser {
    constructor(sampleRate, blockSize) { }
    process(input, spectrum, parameters) {
        // Placeholder: no-op
        return input;
    }
    getLatencySamples() { return 0; }
    reset() { }
}
