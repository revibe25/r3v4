/**
 * MultiTrackPanel — /multitrack route
 * Delegates to MultitrackViewWrapper (the canonical mixer adapter).
 * Both /mixer and /multitrack render the same connected component.
 */
export { default } from '../MultitrackViewWrapper';
