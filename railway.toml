[build]
builder = "nixpacks"
buildCommand = "pnpm install"

[deploy]
startCommand = "pnpm exec drizzle-kit migrate && pnpm exec tsx index.ts"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[deploy.healthcheck]
path = "/api/health"
timeout = 30
interval = 60
